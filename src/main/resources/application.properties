
spring.datasource.url= jdbc:mysql://shop-it.ciqpx0b1tqhj.ap-south-1.rds.amazonaws.com:3306/securitydb?useSSL=false
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=5000




mail.smtp.host=email-smtp.ap-south-1.amazonaws.com
mail.smtp.port=587
mail.smtp.username= ${SMTP_USERNAME}
mail.smtp.password= ${SMTP_PASSWORD}
mail.from= ${SMTP_FROM}
mail.from-name=shiv

mail.verificationapi= ${VERIFICATION_API}
mail.forgotPasswordApi= ${FORGOT_API}


spring.datasource.initialization-mode=always
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

config.oauth2.clientid = ${OAUTH_USERNAME}
config.oauth2.clientSecret = ${OAUTH_PASSWORD}

config.oauth2.publicKey = ${PUBLIC_KEY}
config.oauth2.privateKey = ${PRIVATE_KEY}


